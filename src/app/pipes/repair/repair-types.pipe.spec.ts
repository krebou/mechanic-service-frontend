import { RepairTypesPipe } from './repair-types.pipe';

describe('RepairTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new RepairTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
