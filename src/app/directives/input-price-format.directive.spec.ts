import { InputPriceFormatDirective } from './input-price-format.directive';

describe('PriceFormatDirective', () => {
    it('should create an instance', () => {
        const directive = new InputPriceFormatDirective();
        expect(directive).toBeTruthy();
    });
});
